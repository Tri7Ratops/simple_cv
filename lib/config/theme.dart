import 'package:flutter/material.dart';

class MyAppTheme {
  static ThemeData light() => ThemeData.light().copyWith(
        primaryColor: Color(0XFF54C5F8),
        accentColor: Color(0XFF01579B),
        backgroundColor: Colors.white70,
      );
}
